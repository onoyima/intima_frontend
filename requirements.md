## Packages
framer-motion | Complex page transitions and micro-interactions
date-fns | Date formatting for cycle tracking and messages
lucide-react | Icon system (already in base, but ensuring it's noted)
clsx | Conditional class merging
tailwind-merge | Utility for merging tailwind classes safely

## Notes
Mobile-first design approach with bottom navigation.
Dark mode is the default aesthetic.
Auth is handled via Replit Auth (redirects to /login).
AI endpoints expect JSON body with 'tone' and 'recipientContext'.
Images are static for now, using placeholders.
